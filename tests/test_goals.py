from matches_predictor import prediction


# def test_not_empty_df():
#     df = prediction.get_live_predictions(
#         reprocess=True, retrain=True)
#     assert len(df) != 0
#     df = prediction.get_live_predictions(
#         reprocess=False, retrain=False)
#     assert len(df) != 0
